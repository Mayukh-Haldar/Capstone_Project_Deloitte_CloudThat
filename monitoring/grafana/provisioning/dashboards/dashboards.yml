apiVersion: 1

providers:
  - name: EventZen
    orgId: 1
    folder: EventZen
    folderUid: eventzen
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
